/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com'], // Add the external domain here
  },
  // Add any other configurations you might have
};

export default nextConfig;
